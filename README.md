# send-token
transfer tokens from one address to another address 

import send-token in your project

var sendToken = require('./send-token')

sendToken(privateKey,to,balance,contractAddress,network)

returns the transcation details.
